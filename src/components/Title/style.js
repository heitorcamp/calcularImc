import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  boxTitle:{
    alignItems:"center", //os filhos ficam no centro
    justifyContent:"center", //fica alinhado na horizontal e vertical
  },
  textTitle:{
    color:"#ff0043",
    fontSize:24,
    fontWeight:"bold" // deixa em negrito
  }
});

export default styles